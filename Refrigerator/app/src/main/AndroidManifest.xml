<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mira.refrigerator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ColdTab"
            android:label="@string/title_activity_cold_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FreezeTab"
            android:label="@string/title_activity_freeze_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsTab"
            android:label="@string/title_activity_settings_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddCold" />
        <activity android:name=".AddFreeze"></activity>
    </application>

</manifest>
